<Page x:Class="ATM.Views.BalancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:viewModels="clr-namespace:ATM.ViewModels"
      d:DataContext="{d:DesignInstance viewModels:ATMViewModel}"
      mc:Ignorable="d"
      Title="BalancePage">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Margin="20" Text="{Binding StringFormat=Ваш баланс: {0}, Path=Balance}" />
            <Button Content="Назад" Click="BackClick" />
        </StackPanel>
    </Grid>
</Page>